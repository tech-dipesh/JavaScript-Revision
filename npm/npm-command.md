# i've learn all of this from youtube:
- ```cd /```: Goes homepage
- ```npm  i axios -D``` and ```npm i axios --save-dev``` both are same.
- `npm i axios` install the any library
- `npm un react` or `npm uninstall react` both are same for uninstall any dependancy
- `npm update` or `npm update react`: Update all if no specified or specified that only
- `npm deprecate`: show any list deprecated
- `npm outdated`: check outdated update
- `npm install axios`
    `npm install --save axios`
        `npm i axios`
          `npm i --save axios`
All are same don't need --save nowdays, 
- `npm i -g axios` download globally that next time if we want to use we don't need to import it.
- `npm ls`: show all npm library that we downoad
- `parcel serve index.html -p 8000` or `parcel serve index.html -port 8000` For chanign a pport like this: 
- `npm i cloudinary@latest multer@latest multer-storage-cloudinary@latest`: INstall the latest version
- `npm view axios react`: show what's the recent version, what you have,
- `npm i install cloudinary@2.8.0`: Install the specific version of teh npm
- `rm -rf node_modules package-lock.json`: Remove the files from teh folder
- `npm outdated`: show all outdated npm packagge
- `npm update --save --save-dev`: Update all packages in package.json to latest versions

