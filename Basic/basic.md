Javascript is the interprated language, which is a high level dynamic language.


How three work:
Define a how website structure 
making a layout
define how interactivity and behaviour



- Build tools one of the populat tool is webpack.

- Desktop application is build with the electron framework from js.


- Three js and webrtc for the video and audio p2p connect also js




Javascript engine: Google v8(chrome), chakra(edge), v8(probable )
- v8 is the core of running a javascript, 
with browser manipulation dom, network request done by the api.

- nodejs for the server side programming,


Asynchrounous support which handle non block loop with event loops, callback, and promises





- ecchma script is the standard specification, that sets a core feature not a language




# Javascritp runs at a client side browser, while typescrpt is transpiled to javascript.


  js error detection at a run time while ts error at the compiler time error.





# Js Inspect Feature:
   i: Do real time edit html, css, js
  ii: run scripts
  iii: debug console, 
  iv: modify a dom


The Basic documen



Different Js Console method
1. Console.count for count how many time that number changes.
2. console.time() to check time
3. console.table() 
4. console.info() info way
5. console.dir() display as a heirachy.



# for printing a given value with start 0 and show only non decimal:
use parseInt and then convert that to string and use: padStart(2, '0')