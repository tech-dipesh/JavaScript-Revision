# Function can act like value on js.

Difference on function statement and epxression is: expression is store a function on variable while statement is just normal function statement.
# major difference is statement is act as hoisting while expression is not.


## Function statement is also called function declaration.

Function express mean anonymous function.


## when on the the statemetn function we give function name it's called named function expression.


  - The ablitity to use function as a value or param is called a first class function.


## we called a one function and during function calling we again pass a function that's a first-class-function.



- Higher order argument is a funciton which return/takes from another fucntino as a args called a higher order args.
  -  higher order function is the implment almost same like arr.map with the little checks.


# Default Param:
  - on the deault param, if we don't pass any param it will show a undefined, we can pass the default param, on the argument, if we pass the undefined it's override but it can't overrdie other.
  - It also the destructure.
  - Can also pass a other param as a default param.